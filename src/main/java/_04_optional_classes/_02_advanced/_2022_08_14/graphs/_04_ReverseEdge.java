package _04_optional_classes._02_advanced._2022_08_14.graphs;

public class _04_ReverseEdge {
    // step-1 : add a new reversed edge for every corresponding edge that already exists.
    // step-2 : If weight of already existing (any) edge is considered as 0, then the reverse edges
    // that we are adding will have weight as 1
    // step-3 : afer adding reverse edges, then calculate min cost to travel from
    // 1st node to nth node using BFS 0/1 algorithm
}

/*
given a directed graph with N nodes and E edges, find the minimum number of edges
that needs to be reversed in order to go from 1 to N (1st node to Nth node)
 */